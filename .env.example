# ============================================
# Routa-JS Environment Variables
# ============================================

# ── OpenCode SDK (Recommended for Vercel) ──
# If deploying to Vercel or other serverless platforms,
# you need to run OpenCode server on a VPS or local machine
# and connect to it via the SDK.
#
# 1. On your VPS/local machine, run:
#    opencode serve --host 0.0.0.0 --port 4096
#
# 2. Set this environment variable to your server URL:
OPENCODE_SERVER_URL=http://your-server-ip:4096

# Optional: If your OpenCode server requires authentication
# OPENCODE_SERVER_PASSWORD=your-password-here


# ── API-based Providers (Alternative) ──
# These work natively in serverless environments without
# needing a separate server.

# OpenAI API
# OPENAI_API_KEY=sk-...

# Anthropic Claude API
# ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini API
# GOOGLE_API_KEY=...

# DeepSeek API
# DEEPSEEK_API_KEY=sk-...


# ── Development Settings ──
# For local development, you can use CLI tools directly
# without setting OPENCODE_SERVER_URL. The system will
# automatically detect and use locally installed CLI tools.

# Node environment
NODE_ENV=development

# Next.js settings
# NEXT_PUBLIC_API_URL=http://localhost:3000

